plugins {
    id 'org.hidetake.swagger.generator' version '2.18.2'
}

repositories {
    jcenter()
}

dependencies {
    swaggerUI 'org.webjars:swagger-ui:3.10.0'
}

swaggerSources {
    xjdf {
        inputFile = file('src/specification.yml')
    }
}