schemas:
  Foo:
    type: object
    properties:
      Bar:
        type: array
        items:
          $ref: '#/components/schemas/Bar'
        maxItems: 10
  Bar:
    type: object
    properties:
      att:
        type: string
