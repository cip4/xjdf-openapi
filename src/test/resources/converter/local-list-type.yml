schemas:
  Foo:
    type: object
    properties:
      Bar:
        type: array
        items:
          $ref: '#/components/schemas/Enumeration'
  Enumeration:
    type: string
    pattern: "^[a-zA-Z0-9._\\-:]*$"