$schema: https://json-schema.org/draft/2020-12/schema
type: object
properties:
  Bar:
    type: array
    items:
      type: string
      pattern: "^[a-zA-Z0-9._\\-:]*$"
      enum:
        - V1
        - V2
